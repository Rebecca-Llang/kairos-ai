# Core dependencies
requests>=2.31.0
pyyaml>=6.0.1
termcolor>=2.3.0
sentence-transformers>=2.2.2
torch>=2.0.0
rich>=13.0.0

# Note: torch is required by sentence-transformers (used for AI memory embeddings)
